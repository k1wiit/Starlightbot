# For Discord:

# - for the log's edit them instead of posting new to safe spam

# - extra ticket log for the ticket transscripts

# - welcome message in form of a badge in the welcomechannel -> i'll send you an reference image as example

# - levelsystem -> uses the whitelist to gain level factors like. chatting in both minecraft and discord. the voice time in the voicechannels and the playtime on the minecraft server

# - voicechannel create

# - logs for discord server -> deleted messages get safed

# For Minecraft:

# - /sit command -> can be used by anyone. if the command gets run an invisble armorstand get's spawned under you and you get placed like ina sitting positon on the armorstand. After the player clicks shift to leave the sitting positon the armorstand gets deleted again

# - /playerhead [name] command -> gives you the playerhead of a sertain person you're asking for.

# - /home, /sethome, /delhome, /setpublichome, /delpublichome system -> there are two types of homes. first one is the public home. those are shown for every indivdiual. the secound are the normal private home. those are only shown for the creator. every home gets listed over /home [indivdual home] working over a database that safes those homes for every in divdiual and the public homes are shown aswell. If you run the /hom command without any home an gui gets open. for that i'll provide you another image how that will look. the top bar are the public homes abd the bottom two bars are private homes. the green glass pain on the bottom left is my credit so just a made by K1wit and the other glass pains are nameless gray glasspains. the none set homes are barriers with the custom name: NO HOME PROVIDED -> use /sethome to set your home. and the already set homes are marked with there names and the small description that left click teleports you and right click deletes the home. the set command just works with that you have to hold an item that you want to use so the home is recognisable and the name. the location will be the player location. it will give you an error if you aren't providing both of these things plus if you reach the maximum of 14 homes.
